# 变更日志

记开发中真正要紧的取舍与踩过的坑，不逐条流水。踩坑的详细版在 [docs/PITFALLS.md](docs/PITFALLS.md)，
设计取舍在 [docs/DECISIONS.md](docs/DECISIONS.md)。

## 0.2.0 —— 用户身份：补上机器人看不到的那一半（2026-09-21）

机器人**永远看不到别人私聊你本人的消息**，这是所有 IM 的规则。只做机器人那条线，
归档就只覆盖「主动来找机器人的人」，别人直接发给你的那部分是天生的盲区。
这一版用**同一个应用的第二种身份**把盲区补上。

### 新增

- **用户身份归档线**。你本人授权一次后，定时拉自己的全部会话入库，与机器人那条线
  共用同一个 `handleEvent`，落库行为完全一致。
- **会话发现两条路自动降级**。先试 `listMyChats(types=p2p,group)`（官方文档说列表不含单聊，
  但官方 CLI 实测会传这个参数，**实测可用**）；列不出单聊就退回按已知联系人批量解析单聊
  `chat_id`，降级结果记进 kv，不每轮白试。
- **回传以你本人的名义发出**。对方收到的是真人回复，不是机器人转达。
  用户身份不可用时**降级成机器人发**而不是发不出去——宁可署名变了也不能让话没送到；
  降级时才补回「XX回复：」前缀，并单独告诉主人一声。
- **令牌生命周期**。单写者、自动续期、跟踪官方的 365 天硬顶并提前 30 天预警。
- **重新授权在对话里闭环**。提醒直接带可点的授权链接；点完把浏览器地址栏整条粘回同一个对话，
  机器人自己完成授权。私聊里发「授权」两个字也能随时要一条新链接。
  对话内指令**只认主人、只认私聊、只认这两个形状**。
- **主动告警**。令牌失效、临近重新授权、归档连续失败，都会发消息而不是只写日志。
- `archive.backfill_days`（默认 30）：首次见到一个会话时回溯多久。

### 修复

- **首次归档只拉了十分钟**。`reconcile.overlap_sec` 是给「补断线漏掉的那几条」用的，
  被当成首次归档的起点，结果上线后账面显示「16 个会话成功」，其实 13 个是 0 条。
  这种「数字好看但里面是空的」最容易蒙混过去。
- **告警共用一个冷却**，导致一条连接告警会把更要紧的令牌提醒压掉；且「只提醒」的检查
  排在判死分支之后，连接一挂就提前 return、把令牌提醒一起吞掉。**两个都是测试抓出来的。**
- 附件摘要原来 `readFile` 整份进内存，容器内存受限时会被 OOM，改流式。
- 配置与密钥容忍 UTF-8 BOM（Windows 的 PowerShell 写出来就带，`JSON.parse` 直接抛错，
  而文件肉眼完全正常）。**服务端和命令行是两处独立的读取，第一次只修了一处。**
- 身份校验失败时报出实际看到的 `open_id`。只说「对不上」不说「对不上什么」，这个错没法自查。

### 要知道的三条限制

1. **365 天硬顶**。官方规定：授权满 365 天必须人工重新授权一次，刷新再勤也推不掉。
2. **令牌只能有一个持有者**。`refresh_token` 一次性，换新即作废旧的；两处各存一份会互相顶掉。
3. **单会话历史分页上限** 20 页 × 50 条，回溯太久会被截断。

## 0.1.0 —— 人对人中转（2026-09-20）

别人私聊机器人 → 转发成卡片到你的私聊 → 你回复那条 → 自动回传给对方。
消息与附件归档进 SQLite。默认长连接，webhook 作退路，单容器部署。

### 要点

- 三种事件来源（长连接 / webhook / 定时对账）**共用同一个 `handleEvent`**，
  切换传输方式不改变落库行为。
- `handleEvent` 只去重落库就立刻返回，下载与发送都在异步 worker 里做，
  所以 webhook 的 3 秒超时永远不会被触发。
- **回错人是这类系统最容易出的事故**，所以三条护栏：转发一律用卡片且抬头写清来自谁、
  只认带「回复」的消息、每次回传都回一条带收件人姓名的回执。
- 定时对账兜住长连接断线期丢的事件，并能识别「连接正常却仍在漏」＝疑似有第二个消费者。
- 附件三道闸：单文件上限、软硬配额、磁盘剩余下限。
- `node:sqlite`，无原生模块；运行时依赖只有官方 SDK。

### 踩过的坑（详见 PITFALLS）

Windows 上自启守卫恒假导致容器空转且日志安静；Dockerfile 的 apt 层多余且脆；
PowerShell 的 `$Args` 撞自动变量；`.ps1` 无 BOM 时 PowerShell 5.1 按 ANSI 解。
